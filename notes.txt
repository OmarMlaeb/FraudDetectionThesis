1. Create a virtual environment
On macOS/Linux:
python3 -m venv venv

On Windows:
python -m venv venv

2. Activate the virtual environment
On macOS/Linux:
source venv/bin/activate

On Windows:
venv\Scripts\activate

////////////////////////
cd fraud-detection-thesis

python3 -m venv venv

source venv/bin/activate

pip install --upgrade pip

pip install -r requirements.txt

Then install PyTorch separately with:
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

to test if gpu is recognized:
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"

//////////////////////
IEEE-CIS dataset:

To run every IEEE-CIS model in order:
python src/run_ieee_all.py

For GNN baselines, install PyTorch Geometric after PyTorch:
pip install torch-geometric

//////////////////////
Elliptic Bitcoin dataset:

To run every Elliptic model in order:
python src/run_elliptic_all.py
